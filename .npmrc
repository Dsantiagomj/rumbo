auto-install-peers=true
strict-peer-dependencies=false

# Required for React Native / Expo compatibility with pnpm
# Metro bundler cannot resolve pnpm's symlinked node_modules structure
node-linker=hoisted
